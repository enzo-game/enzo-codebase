# 峽谷行者 · 卡面美術生圖進度報告（給 Codex）

> 這份是卡面圖（card art）唯一的權威現況與任務對齊文件。若你的紀錄還停在 **ORDER-17**，請以本報告為準——ORDER-17 早已被後續的卡池大改版取代，跟現在的生圖任務無關。

## TL;DR
- 卡池已擴到 **956 張**；其中 **809 張新卡需要卡面圖**。
- 任務清單＝ **`docs/card-art/` 資料夾裡的小塊檔**（每檔 ~30 張，共 28 檔）＋ `docs/card-art/README.md` 索引。**一次做一檔就好**（原本每批 150–194 張一次太多會當機，已拆小）。
- 目前進度：**已生 ~208 / 809**。批次 1 全數完成、批次 2 部分、批次 3–5 未開始。
- **做法：開一個小塊檔 → 生完裡面每一張（`<id>.jpg` 已存在就跳過）→ 換下一檔。** 批次 2 的第一個小塊 `batch-2-part-01.md` 起。
- ✅ 已生的 208 張圖工程端已登錄 `CARD_ART`（批次 1 卡面在遊戲裡已是真圖）；之後你每生一批，工程端重跑 `gen-cardart.mjs` 增量登錄。

## 為什麼你「卡在 ORDER-17」
ORDER-17 是很早以前的舊任務。之後卡表經歷大改版：v1 卡全數淘汰（文化紅線盤點）、改綁 Truku 詞庫、出牌答題改成隨機出題、接著分 **5 批**把卡池從 147 擴充到 956。跟卡面圖有關的**唯一權威來源**就是下面這 5 份 MD，請忽略 ORDER-17 的舊狀態。

## 任務清單與進度（權威）
清單改放 **`docs/card-art/`**（拆成 28 個小塊檔，每檔 ~30 張），索引見 `docs/card-art/README.md`。舊的大檔 `card-art-batch-1..5.md` 已移除。

| 批次 | 小塊檔 | 需生 | 已生 | 缺 |
|---|---|---:|---:|---:|
| 1 | `batch-1-part-01..07.md` | 194 | 194 | 0 ✅ |
| 2 | `batch-2-part-01..06.md` | 175 | ~14 | ~161 |
| 3 | `batch-3-part-01..05.md` | 150 | 0 | 150 |
| 4 | `batch-4-part-01..05.md` | 140 | 0 | 140 |
| 5 | `batch-5-part-01..05.md` | 150 | 0 | 150 |
| **合計** | | **809** | **~208** | **~601** |

## 規格（每份 MD 開頭也有）
- **檔名**：每張存成 `public/images/cards/<id>.jpg`。id 見 MD 的「檔名」欄，例如 `gen-0201.jpg`、`leg-l40.jpg`。**檔名一定要跟 id 一致**，否則工程端登錄不到。
- **風格**：厚塗寫實／水彩感的台灣山林插畫，5:7 直式，構圖飽滿，無文字、無邊框、無浮水印。
- **文化框限**：不畫人形、不畫神聖器物或祭儀場景、不畫百步蛇。傳說卡（`leg-*`）另有個別提示，一律謹守地景／自然、不畫人形。
- **id 穩定**：批次順序與 id 固定，不會變動 → **批次 1 已生的 194 張不需重做**。

## 接下來請做（Codex）
1. 打開 `docs/card-art/README.md`，**從 `batch-2-part-01.md` 開始，一次做一個小塊檔**（每檔 ~30 張，不會過載）。
2. 生該檔列出的每一張，嚴格用 `<id>.jpg` 檔名存到 `public/images/cards/`；**同名檔已存在就跳過**。
3. 做完一個小塊檔就換下一個，依序把批次 2 → 5 生完。每完成幾檔就推上來／告知工程端登錄 `CARD_ART`（圖才會在遊戲顯示）。

## 工程端（enzo-codebase）的對應待辦
- 目前 `CARD_ART` 只登錄了原本 147 張；**已生的 208 張新圖尚未登錄**（所以遊戲裡還是佔位圖）。工程端會把已完成的批次陸續**壓縮＋登錄**，讓卡面實際換成真圖。
- 之後量大時，卡圖可改放物件儲存／CDN（如 Cloudflare R2，出站流量零費用）以控成本；資料層（`match_state`）已 id 化，卡池變大不影響伺服器負擔。

_更新時間：卡池 956、已生 208/809。_
